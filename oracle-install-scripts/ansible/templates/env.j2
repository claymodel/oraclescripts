# env.sh
# This gets sourced by bash_profile and all scripts in ~/bin need it
# Put all environment var that you need in this file.

export ORACLE_HOME={{oracle_home}}
export ORACLE_SID={{oracle_sid}}
export MW_HOME={{mw_home}}
export DOMAIN_LIST="{{domain_list}}"
export JAVA_HOME={{java_home}}
export OUD_HOME=$MW_HOME/asinst_1/OUD

